import { MyinterceptorInterceptor } from './myinterceptor.interceptor';

describe('MyinterceptorInterceptor', () => {
  it('should be defined', () => {
    expect(new MyinterceptorInterceptor()).toBeDefined();
  });
});
